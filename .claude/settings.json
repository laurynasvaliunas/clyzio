{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/LaurynasValiunas/Documents/clyzio/clyzio-app && git add -A && (git diff --cached --quiet || git commit -m \"chore: save session changes\") && git push 2>&1 || true",
            "timeout": 30,
            "statusMessage": "Pushing changes to git..."
          }
        ]
      }
    ]
  }
}
